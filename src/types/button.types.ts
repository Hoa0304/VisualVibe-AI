export interface ButtonProps {
    children: React.ReactNode;
    onClick?: () => void;
    bgColor?: string;
    textColor?: string;
    width?: string;
    height?: string;
    borderRadius?: string;
}
